using Xamarin.Forms;

// Main page of the app that populates the tabs
// This page isn't actually seen as it is the controller for the tabs

namespace UEApp
{
    public partial class MainPage : TabbedPage
    {
        public MainPage()
        {
            InitializeComponent();
        }
    }
}
